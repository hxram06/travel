# 유럽 여행 계획 — 규칙

이 문서는 **일정을 어떻게 만들 것인가**에 대한 규칙만 담는다.
실제 일정 데이터는 `data.js`에, 사진 목록은 `photos-data.js`에 있다.

일정을 바꿀 때는 `data.js`만 고치면 되고, 이 문서의 규칙을 지켰는지는
`rules.js`가 페이지 로드 시 자동으로 검사해 브라우저 콘솔에 알려준다.

---

## 파일 역할 분리

| 파일 | 역할 | 일정 변경 시 |
|---|---|---|
| `RULES.md` | 규칙 문서 (사람이 읽는 기준) | 규칙 자체가 바뀔 때만 |
| `rules.js` | 규칙의 기계 검사판 (자동 검증) | 규칙 자체가 바뀔 때만 |
| `data.js` | **일정 데이터** | ✅ 여기만 고치면 됨 |
| `photos-data.js` | 사진 URL·저작자·라이선스 | 새 사진이 필요할 때 |
| `map.js` / `app.js` / `styles.css` | 화면과 동작 | 건드릴 일 거의 없음 |

---

## 규칙 1 — 사진은 "그날 보거나 먹는 것"을 그 자리에 놓는다

**핵심 원칙: 그날 일정에 실제로 등장하는 것만, 실제 위치에.**

- 그날 **보는 것**(명소·건물·풍경)과 **먹는 것**을 사진으로 넣는다.
- 먹는 것은 **음식 단독 사진보다 그 음식을 파는 유명 음식점**을 우선한다.
  가게 사진이 없으면 대표 메뉴 사진을 쓰되, 설명에 가게 이름과 내력을 적는다.
  - 예: 빈 5일차 → 카페 자허(1876년, 오리지널 자허토르테) + 자허토르테
  - 예: 마드리드 1일차 → 소브리노 데 보틴(1725년, 세계 최고령 레스토랑) + 코치니요
- 각 사진에는 `desc`를 반드시 넣는다. 팝업에 표시되는 **장소·음식·음식점 설명**이며,
  40자 이상 쓴다. 무엇인지, 왜 유명한지, 알아두면 좋은 점을 담는다.
- 사진의 `at` 좌표는 **그 명소의 실제 위치**여야 한다. 도시 중심 좌표를 재사용하지 않는다.
  - 예: 바르셀로나 4일차 → 사그라다 파밀리아는 `[2.1744, 41.4036]`, 구엘 공원은 `[2.1527, 41.4145]`
  - 예: 파리 12일차 → 에펠탑은 `[2.2945, 48.8584]`, 루브르는 `[2.3376, 48.8606]`
- 음식 사진의 `at`은 그 음식을 먹는 동네나 식당가 좌표를 쓴다.
- 그날 일정(`am`/`pm`/`ev`)에 **언급되지 않은 것**은 넣지 않는다.
- 한 날에 **2~12장**을 허용한다. 기본 코스는 2~5장이 적당하지만, 코스 9처럼 사진 풍부 모드가 필요한 경우에는 10장 이상도 허용한다.
- **한 코스 안에서 같은 사진 URL이나 같은 로컬 파일을 재사용하지 않는다.**
  같은 장소를 다시 보여줘야 해도 다른 실제 사진을 쓴다.
- 사진은 우측 패널에 넣지 않는다. **지도 마커로만** 표시한다.

### 사진 추가 방법
1. `photos-data.js`에 항목을 추가한다 (URL·저작자·라이선스·원본 페이지).
2. `data.js`의 해당 날짜 `photos` 배열에 아래 형태로 넣는다.

```javascript
{ spot: 'r_cafe_sacher', at: [16.3697, 48.2036], cap: '카페 자허 — 1876년',
  desc: '호텔 자허에 딸린 카페로 오리지널 자허토르테를 파는 곳이다. ...' }
```

`cap`은 한 줄 제목, `desc`는 팝업에 나오는 설명이다. 음식점 키는 `r_`로 시작한다.

> ⚠️ **URL을 지어내지 말 것.** 검사기가 실제로 이미지를 불러와 확인하며,
> 열리지 않으면 콘솔에 「열리지 않는 사진」으로 표시된다.
> 위키미디어 커먼즈에서 실제 파일을 찾아 그 URL을 써야 한다.

### 사진 출처 규칙
- **위키미디어 커먼즈 / 위키백과의 자유 이용 라이선스 사진만** 사용한다.
- 저작자와 라이선스를 `credit`에 반드시 적고, 원본 파일 페이지를 `source`에 남긴다.
- 이 정보는 사진 팝업 하단에 표시되며 클릭하면 원본으로 연결된다.
- 로고·국기·지도·문장(紋章) 이미지는 쓰지 않는다.

---

## 규칙 2 — 이동은 실제 경로를 따라간다

- 모든 날짜에 `transport: { mode, label }`이 있어야 한다.
- `mode`는 `plane` / `train` / `bus` / `ferry` / `walk` 중 하나다.
- `label`에는 **구간과 소요 시간, 주요 경유지**를 적는다.
  - 예: `'프라하 → 빈 · Railjet 4시간 (브르노 경유)'`
- **지상·해상 이동(train/bus/ferry)은 `via` 경유지 좌표를 반드시 넣는다.**
  직선으로 가로지르지 않고 실제로 지나는 도시·터널·해협을 거쳐야 한다.
  - 예: 루체른 → 밀라노는 고타르트 터널 → 벨린초나 → 루가노 → 코모를 거친다.
  - 예: 런던 → 파리 유로스타는 애슈퍼드 → 칼레 → 릴을 거친다.
- 비행(`plane`)은 `via`가 필요 없다. 지구 곡률을 따르는 대권 곡선이 자동 생성된다.
- 같은 도시에 머무는 날은 `mode: 'walk'`에 시내 이동 수단을 적는다.

---

## 규칙 3 — 당일치기는 반드시 돌아온다

- `isTrip: true`인 날은 `baseCity`와 `baseCoords`(숙박 도시)가 있어야 한다.
- 패널에 `★ 당일치기` 배지와 "저녁에 ○○로 귀환합니다" 문구가 자동으로 붙는다.
- 다음 날로 넘어갈 때 **숙박 도시로 먼저 돌아간 뒤** 다음 목적지로 이동한다.
  (이 동작은 `map.js`가 자동 처리하므로 일정에는 귀환 날짜를 따로 만들지 않는다.)

---

## 규칙 4 — 일정 설명은 충분히 쓴다

- `am` / `pm` / `ev` 각각에 **그날 실제로 하는 일**을 문장으로 쓴다.
- 단순 나열이 아니라 **왜 그렇게 하는지, 무엇을 보게 되는지**를 담는다.
- 세 항목을 합쳐 최소 120자 이상, 240자 이상 권장 (현재 일정의 중앙값은 약 290자).
- `tip`에는 예약 필요 여부, 주의사항, 대안을 적는다.
- 마지막 귀국일도 예외 없이 설명을 채운다.

---

## 규칙 5 — 날짜 구조

- `day`는 1부터 순서대로, 배열 순서와 일치해야 한다.
- `moveType`은 `flight` / `ground` / `stay` 중 하나다.
- 각 코스의 마지막 날은 **귀국 출발일**로 둔다 (`transport.mode: 'plane'`).
- 마지막 날 패널에는 **인천 귀국** 버튼이 자동으로 나타난다.
  누르면 귀국 비행 애니메이션이 재생되므로, 인천 도착을 위한 날짜를 따로 만들지 않는다.

---

## 규칙 6 — 코스 메타데이터

각 코스에는 다음이 있어야 한다.

| 필드 | 설명 |
|---|---|
| `id` | 코스 번호 |
| `nameKo` | 코스 이름 |
| `subtitle` | 한 줄 설명 |
| `period` | 시기 |
| `nights` | 「N박 N일」 — 실제 날짜 수와 맞아야 한다 |
| `color` | 코스 색상 (카드 배지·진행 바에 사용) |
| `cities` | 주요 도시 목록 (당일치기 목적지는 제외) |
| `coverSpot` | 카드 배경 사진 — `photos-data.js`의 키 |

---

## 일정을 바꿀 때 체크리스트

1. `data.js`에서 해당 날짜를 고친다.
2. `day` 번호가 1부터 연속인지 확인한다.
3. 도시가 바뀌었으면 `coords`, `transport.label`, `via` 경유지를 함께 고친다.
4. 사진을 그 날짜에 맞게 교체한다 (규칙 1).
5. 코스의 `nights`와 `cities`를 확인한다.
6. 페이지를 새로고침하고 **브라우저 콘솔**(F12)을 본다.
   `rules.js`가 위반 사항을 표로 출력한다. 통과하면 `✅ 규칙 검사 통과`가 뜬다.

---

## 검사기가 잡는 것과 못 잡는 것

`rules.js`는 **형식**을 검사한다. 아래는 자동으로 잡힌다.

- 필드 누락, 날짜 번호 어긋남, 허용되지 않는 `moveType`·이동수단
- 사진 키가 `photos-data.js`에 없음, 저작자·출처·`desc` 누락
- 같은 코스 안에서 동일한 사진 URL·로컬 파일을 재사용한 경우
- **사진 URL이 실제로 열리지 않음** (지어낸 URL 방지)
- **경도·위도를 바꿔 넣은 좌표** (유럽 범위를 벗어나는데 뒤집으면 맞는 경우)
- **경유지가 출발–도착 회랑을 크게 벗어남** (우회율이 3.2배를 넘거나 900km 이탈)
- 사진이 그날 동선에서 너무 멂, 설명이 너무 짧음
- `data.js`에 문법 오류가 나서 데이터를 아예 못 읽는 경우 (화면에 경고 표시)

**아래는 자동으로 잡히지 않으니 사람이 확인해야 한다.**

- 사진이 그날 일정과 **의미상** 맞는지 (좌표만 맞으면 통과한다)
- 이동 시간·노선·영업 여부 같은 **사실 관계**
- 라이선스 표기가 실제로 정확한지 (문자열이 있는지만 본다)
- 설명 내용이 맞는지
