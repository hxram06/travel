# Travel Map Project - AI Agent Guidelines

이 가이드라인은 프로젝트에 새로운 장소(Course/Day)나 사진 데이터를 추가할 때 AI(Agent)가 반드시 지켜야 할 원칙입니다.

## 1. 사진(Photos) 추가 및 생성 규칙
* **정확한 위치 (100m 룰):** 모든 사진 데이터의 `at`(좌표: `[경도, 위도]`) 값은 해당 사진이 찍힌 실제 랜드마크, 혹은 사진에 묘사된 랜드마크의 **실제 위치 반경 100m 이내**의 정확한 좌표여야 합니다.
* **무작위 좌표 금지:** 강 위, 바다 위, 임의의 도로 위에 랜덤하게 핀을 찍는 행위를 엄격히 금지합니다. 좌표는 위키데이터(Wikidata)나 지도 API를 통해 정밀하게 검증해야 합니다.
* **고품질 사진 우선:** 위키백과의 칙칙한 사진보다는 가급적 인스타그램 감성의 화사하고 미적으로 우수한 고화질 여행 사진(여행 블로그, Unsplash, Pexels 등)을 우선적으로 수집하여 사용합니다.
* **설명 불일치 금지:** 사진의 시각적 내용(`url`), 제목(`cap`), 설명(`desc`)은 100% 일치해야 하며, "관련 없는 피자", "소포 봉투" 같은 쓰레기 데이터 삽입을 절대 금지합니다.

## 2. 지도 뷰포트 (Map Viewport) UI 규칙
* **패딩(Padding) 동적 계산:** `map.js`에서 지도 시점 이동(`flyTo`, `fitBounds`)을 수행할 때는 반드시 UI 패널(데스크톱의 경우 우측 `#panel`, 모바일의 경우 하단 바텀시트)의 너비나 높이를 계산하여 `padding` 값에 반영해야 합니다. 
* **시스템의 중심 이동:** 마커와 경로의 중심이 화면 정중앙이 아닌, 패널을 제외한 나머지 여백의 중앙에 오도록 유지해야 합니다.
